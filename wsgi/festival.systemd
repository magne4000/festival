[Unit]
Description=Festival Scanner
After=network.target

[Service]
User={same user as the one configured in apache WSGI}
Group={same group as the one configured in apache WSGI}
ExecStart=/usr/bin/python3 scanner.py
WorkingDirectory={/path/to/festival}

[Install]
WantedBy=multi-user.target